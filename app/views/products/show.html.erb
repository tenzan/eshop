<% title(@product.name)%>

<header>
  <h1><%= @product.name %></h1>

  <ul class="actions">
    <li>
      <%= link_to 'Edit Product',
      edit_product_path(@product), class: "edit" %>
    </li>
    <% admins_only do  %>
    <li>
      <%= link_to'Delete Product',
      admin_product_path(@product),
      method: :delete,
      data: { confirm: "Are you sure you want to delete this product?"},
      class: "delete" %>
    </li>
    <% end %>
  </ul>
</header>

<p><%= @product.description %></p>
<p><%= @product.price %></p>
